name: ory

x-hardening: &hard
  read_only: true
  tmpfs: ["/tmp:rw,noexec,nosuid,size=32m"]
  security_opt: ["no-new-privileges:true"]
  cap_drop: [ALL]

services:
  kratos-migrate:
    <<: *hard
    container_name: kanae_kratos_migrate
    image: oryd/kratos:v26.2.0
    environment:
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      DSN: postgres://${DB_USERNAME}:${DB_PASSWORD}@database:5432/kratos?sslmode=disable&max_conns=20&max_idle_conns=4
      SECRETS_COOKIE: ${KRATOS_SECRETS_COOKIE}
      SECRETS_CIPHER: ${KRATOS_SECRETS_CIPHER}
    command: migrate sql -c /etc/config/kratos.yml --read-from-env --yes
    networks:
      - default
      - db_bridge
    volumes:
      - ./config/kratos:/etc/config:ro
    depends_on:
      database:
        condition: service_healthy
    restart: on-failure

  kratos:
    <<: *hard
    container_name: kanae_kratos
    image: oryd/kratos:v26.2.0
    environment:
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      DSN: postgres://${DB_USERNAME}:${DB_PASSWORD}@database:5432/kratos?sslmode=disable&max_conns=20&max_idle_conns=4


      SECRETS_COOKIE: ${KRATOS_SECRETS_COOKIE}
      SECRETS_CIPHER: ${KRATOS_SECRETS_CIPHER}

      SELFSERVICE_FLOWS_REGISTRATION_AFTER_PASSWORD_HOOKS_0_CONFIG_AUTH_CONFIG_VALUE: ${KRATOS_WEBHOOK_TOKEN_REGISTRATION}
      SELFSERVICE_FLOWS_SETTINGS_AFTER_PASSWORD_HOOKS_0_CONFIG_AUTH_CONFIG_VALUE: ${KRATOS_WEBHOOK_TOKEN_SETTINGS}
      SELFSERVICE_FLOWS_SETTINGS_AFTER_PROFILE_HOOKS_0_CONFIG_AUTH_CONFIG_VALUE: ${KRATOS_WEBHOOK_TOKEN_SETTINGS}

      SERVE_PUBLIC_BASE_URL: ${KRATOS_SERVE_PUBLIC_BASE_URL:-${KRATOS_PUBLIC_ORIGIN}/auth}
      SELFSERVICE_DEFAULT_BROWSER_RETURN_URL: ${KRATOS_PUBLIC_ORIGIN}/
      SELFSERVICE_ALLOWED_RETURN_URLS_0: http://localhost:5173
      SELFSERVICE_ALLOWED_RETURN_URLS_1: ${KRATOS_PUBLIC_ORIGIN:?}
      SELFSERVICE_FLOWS_LOGIN_UI_URL: ${KRATOS_PUBLIC_ORIGIN}/login
      SELFSERVICE_FLOWS_REGISTRATION_UI_URL: ${KRATOS_PUBLIC_ORIGIN}/register
      SELFSERVICE_FLOWS_SETTINGS_UI_URL: ${KRATOS_PUBLIC_ORIGIN}/dashboard/settings
      SELFSERVICE_FLOWS_ERROR_UI_URL: ${KRATOS_PUBLIC_ORIGIN}/error
      SELFSERVICE_FLOWS_VERIFICATION_UI_URL: ${KRATOS_PUBLIC_ORIGIN}/verify
      SELFSERVICE_FLOWS_RECOVERY_UI_URL: ${KRATOS_PUBLIC_ORIGIN}/recovery
      SELFSERVICE_FLOWS_LOGOUT_AFTER_DEFAULT_BROWSER_RETURN_URL: ${KRATOS_PUBLIC_ORIGIN}/login

    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: serve -c /etc/config/kratos.yml --watch-courier
    ports:
      - "4433:4433"
      - "127.0.0.1:4434:4434"
    networks:
      - default
      - db_bridge
    volumes:
      - ./config/kratos:/etc/config:ro
    depends_on:
      kratos-migrate:
        condition: service_completed_successfully
    restart: unless-stopped

  keto-migrate:
    <<: *hard
    container_name: kanae_keto_migrate
    image: oryd/keto:v26.2.0
    environment:
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      DSN: postgres://${DB_USERNAME}:${DB_PASSWORD}@database:5432/keto?sslmode=disable&max_conns=20&max_idle_conns=4
    command: migrate up -c /etc/config/keto.yml --yes
    networks:
      - default
      - db_bridge
    volumes:
      - ./config/keto:/etc/config:ro
    depends_on:
      database:
        condition: service_healthy
    restart: on-failure

  keto:
    <<: *hard
    container_name: kanae_keto
    image: oryd/keto:v26.2.0
    environment:
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      DSN: postgres://${DB_USERNAME}:${DB_PASSWORD}@database:5432/keto?sslmode=disable&max_conns=20&max_idle_conns=4
    command: serve -c /etc/config/keto.yml
    ports:
      - "127.0.0.1:4466:4466"
      - "127.0.0.1:4467:4467"
    networks:
      - default
      - db_bridge
    volumes:
      - ./config/keto:/etc/config:ro
    depends_on:
      keto-migrate:
        condition: service_completed_successfully
    restart: unless-stopped

  mailpit:
    <<: *hard
    container_name: kanae_mailpit
    image: axllent/mailpit:latest
    ports:
      - "127.0.0.1:8025:8025"
      - "127.0.0.1:1025:1025"
    networks:
      - default
    healthcheck:
      test: /mailpit readyz
      interval: 30s
      timeout: 3s
      start_period: 5s
      start_interval: 1s
    restart: unless-stopped
